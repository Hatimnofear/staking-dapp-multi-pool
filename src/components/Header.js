import React from 'react';
import WalletConnect from '../context/WalletConnect';

const Header = ({ }) => {
  return (
    <header>
      <WalletConnect />
    </header>
    
  );
};

export default Header;
